apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - service_account.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/milo-os/stripe-provider
    newTag: dev
